name: habit_tracker_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjusted to be compatible with latest Flutter/Dart versions

dependencies:
  flutter:
    sdk: flutter

  # iOS-style icons
  cupertino_icons: ^1.0.8

  # For internationalization
  intl: ^0.18.1
  percent_indicator: ^4.2.3 # Updated to match common latest version
  uuid: ^4.2.1 # Added the uuid package - THIS IS CRUCIAL FOR THE APP TO WORK

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # Uncomment and add your assets if needed
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # Uncomment and add fonts if needed
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #       - asset: fonts/Roboto-Bold.ttf
  #         weight: 700
